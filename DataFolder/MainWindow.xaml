<Window x:Class="DataFolder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataFolder"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" MinWidth="500">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" MinWidth="130"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!--Quellordner-->
		<TextBlock x:Name="TextFolderSource" Grid.Column="0" Grid.Row="0" Text="Quellordner" ToolTip="In diesem Ordner sind die unsortierten Dateien gespeichert."/>
		<TextBox x:Name="InputFolderSource" Grid.Column="1" Grid.Row="0" />
		<Button x:Name="ButtonFolderSource" Grid.Column="2" Grid.Row="0" Content="Öffne Ordner" Click="ButtonFolderSource_Click"/>

    <TextBlock x:Name="TextSubFolder" Grid.Column="1" Grid.Row="1" Text="Unterordner berücksichtigen"/>
	</Grid>
</Window>
